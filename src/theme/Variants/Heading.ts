import { defineStyle } from '@chakra-ui/react';

export const chatHeading = defineStyle({
  size: '48px',
  color: 'white',
  fontFamily: 'Montserrat, sans-serif',
  textAlign: 'center'
});

export const authHeading = defineStyle({
  size: '48px',
  color: 'white',
  fontFamily: 'Inter, sans-serif',
  textAlign: 'center'
});
